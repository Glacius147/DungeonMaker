//Randomize frame, animation speed, direction, speed and size
image_index = random(4);
image_speed = choose(0.5,0.75);
speed = random_range(-3,3);
direction = random(360);
image_xscale = choose(-1,1);
image_yscale = choose(-1,1);